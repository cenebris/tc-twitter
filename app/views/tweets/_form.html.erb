<%= form_for @tweet, :html => { :class => "form-horizontal tweet" } do |f| %>

    <% if @tweet.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@tweet.errors.count, "error") %> prohibited this tweet from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @tweet.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="control-group">
    <%= f.label :body, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :body, :class => 'form-control' %>
    </div>
    <%= error_span(@tweet[:body]) %>
  </div>

<div class=""
  <%= f.submit nil, :class => 'btn btn-lg btn-primary #{ApplicationController::BUTTON_HOVER}' %>
  <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
            tweets_path, :class => 'btn btn-lg btn-default #{ApplicationController::BUTTON_HOVER}' %>

<% end %>
