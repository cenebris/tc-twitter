<!DOCTYPE html>
<html>
<head>

  <% if user_signed_in? %> <!-- Show username in title if user is signed in -->
  <title>TC Twitter - <%= current_user.username %> </title>
  <% else %>
  <title>TC Twitter</title>
  <% end %>

  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
  <meta name="viewport" content="width=device-width, initial-scale=1">
</head>

<body>

<%= render 'layouts/menu' %>

<%= yield %>

</body>
</html>
